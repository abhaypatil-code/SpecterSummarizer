# Core ML stack (CPU-friendly; install GPU torch separately if available)
torch>=2.2
transformers>=4.40
accelerate>=0.26
datasets>=2.18
sentencepiece>=0.2
numpy>=1.26,<2.3

# Summarization training metrics (optional but used by the README workflow)
rouge-score>=0.1.2

# Local retrieval-augmented chat (no external API)
sentence-transformers>=3.0
faiss-cpu>=1.8

# App + PDF parsing
streamlit>=1.36
pypdf>=4.0
