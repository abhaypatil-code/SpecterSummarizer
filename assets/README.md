# Assets

Put demo screenshots here and reference them from the project README:

- `summary.png` — the Summary tab with a generated summary
- `chat.png` — the Chat tab answering a question about a judgment

Capture them by running `streamlit run app.py`, loading the sample judgment,
and taking screenshots of each tab.
